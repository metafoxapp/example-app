/**
 * @type: route
 * name: note.home
 * path: /note
 */

import { createLandingPage } from '@metafox/framework';

export default createLandingPage({
  appName: 'note',
  pageName: 'note.home',
  resourceName: 'note'
});
