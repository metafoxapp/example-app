/**
 * @type: route
 * name: note.home
 * path: /note
 */

import { createLandingPage } from '@metafox/core';

export default createLandingPage({
  appName: 'note',
  pageName: 'note.home',
  resourceName: 'note'
});
